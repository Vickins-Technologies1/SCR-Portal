# Copy to `android/keystore.properties` (DO NOT COMMIT) and fill in values.
#
# Store file should be a path relative to `android/` (recommended) or an absolute path.
storeFile=keystore/sorana-release.jks
storePassword=CHANGE_ME
keyAlias=sorana
keyPassword=CHANGE_ME

